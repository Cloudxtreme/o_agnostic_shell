#!/usr/bin/env sh
#
# prints cpu frequency in hertz

# bsd
sysctl -n machdep.tsc_freq 2>/dev/null ||

# linux

( lscpu |
  grep --ignore-case "mhz" |
  awk '{ print $3 + $4 }' |
  sort |
  head -1 
) 2>/dev/null ||

exit 1 #fail

exit 0 #success
